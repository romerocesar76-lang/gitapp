>> git ini
# inicia git

frontend = software

backend = base de datos

>> git add "nombre archivo"
# comienza a seguir al arvhivo a git

>> git add .
# comienza a seguir a todos los archivos

>> git commit -m "Nota..."
# hace el commit. "saca la foto"

el archivo .gitignore
# es el archivo que contiene todos los archivos o carpetas que no necesito subir al repositorio.

>> git log
# muestra lo que hicimos

>> git remote add "https://direccion del repositorio"
# Vincula mis archivos con el repositorio

>> git push origin main
# Un push en Git es la acción de enviar (subir) tus commits desde tu repositorio local a un repositorio remoto, como GitHub.
>> git pull origin main
# Es el comando que se usa para traer cambios del repositorio remoto a tu repositorio local (por ejemplo desde GitHub) y aplicarlos automáticamente.
# main nombre de la rama

>> git remote -v
# Nos muetra la direccion del repositorio.




